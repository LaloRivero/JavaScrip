var precioDeVino = 200.135;
var total = Math.round(precioDeVino * 100 * 3) / 100;
var totalStr = total.toFixed(2); //Se crea un string
var total2 = parseFloat(totalStr);//Se convierte en un float

var pizza = 8;
var persona = 2;
var cantDePizza = pizza / persona;

